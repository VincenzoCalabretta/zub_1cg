"""Bare-metal BSP smoke test for AES-ZUB Cortex-R5F (no ThreadX)."""

load("//sdk/rules:defs.bzl", "r5_firmware")

r5_firmware(
    name = "bsp_test",
    srcs = ["main.c"],
    bsp = "//sdk/bsp/rpu:bsp_bare",
    visibility = ["//visibility:public"],
    deps = ["//sdk/bsp:test_proto"],
)
